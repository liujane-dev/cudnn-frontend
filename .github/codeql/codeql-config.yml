name: CodeQL config

queries:
  - uses: security-extended
  - uses: security-and-quality

paths-ignore:
  # Vendored or generated upstream code is intentionally kept byte-for-byte.
  - include/cudnn_frontend/thirdparty/**
  - python/cudnn/moe_ep/_megamoe_backend/cutedsl_src/**
  - python/cudnn/linear_attention/cake/kernels/**
